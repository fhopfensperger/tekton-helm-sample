apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-sample-chart.fullname" . }}-test-connection"
  labels:
    {{- include "helm-sample-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-sample-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
